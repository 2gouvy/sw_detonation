%flow parameters
gamma=1.4;
M1=1.3;

%limits of plot
xilim=xi_lim(M1,gamma)
xidm=xi_delta_max(M1,gamma);
tdeltam=sqrt(tan_def_sq(xidm,M1,gamma))
deltam=atan(tdeltam)
ny=100;%number of xi points
xi_log_plot_step=(xilim)^(1/ny);
xi_axis=xi_log_plot_step.^(1:ny);

%polar plot
delta_pos=atan(sqrt(tan_def_sqrt(xi_axis,M1,gamma)));
delta_neg=-delta_pos;
plot([delta_neg,delta_pos
